{
    "title": "User",
    "type": "object",
    "description": "A Interviewer user.",
    "properties": {
        "firstName": {
            "type": "string"
        },
        "lastName": {
            "type": "string"
        },
        "email": {
            "type": "string",
            "minLength": 5,
            "description": "Email address of a user."
        },
        "password": {
            "type": "string",
            "minLength": 4,
            "description": "Plain text password of a user."
        }
    },
    "required": ["email", "password"]
}